{
  "name": "thiengtham-website",
  "version": "1.0.0",
  "description": "Thiengtham Development - รับเหมาซ่อมบ้านและคอนโด",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.7.0",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.8"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
